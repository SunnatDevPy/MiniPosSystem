services:
  backend:
    build: ./backend
    container_name: mini-pos-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app

  frontend:
    build: ./frontend
    container_name: mini-pos-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
